"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const command_1 = require("@oclif/command");
const devcert_1 = require("devcert");
class Generate extends command_1.Command {
    async run() {
        const { args } = this.parse(Generate);
        if (!args.domain) {
            this.error('You must supply a domain name that you want to disable.');
        }
        if (!devcert_1.hasCertificateFor(args.domain)) {
            this.error(`${args.domain} doesn't appear to be configured yet.`);
        }
        devcert_1.removeDomain(args.domain);
        this.log(`${args.domain} deleted`);
    }
}
Generate.description = 'delete the cached certificate for a domain';
Generate.flags = {};
Generate.args = [{ name: 'domain' }];
exports.default = Generate;
